<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>SQL Tabela Estados</comment>

	<entry key="sql.update.estado">
		UPDATE ESTADOS SET
			ESTADO = ?
		WHERE
			IDESTADO = ?
	</entry>

	<entry key="sql.delete.estado">
		DELETE FROM
			ESTADOS
		WHERE
			IDESTADO = ?
	</entry>

	<entry key="sql.select.estado">
		SELECT * FROM ESTADOS
	</entry>

	<entry key="sql.select.cod.estado">
		SELECT * FROM ESTADOS WHERE IDESTADO = ?
	</entry>

	<entry key="sql.select.name.estado">
		SELECT * FROM ESTADOS WHERE ESTADO LIKE ?
	</entry>

</properties>